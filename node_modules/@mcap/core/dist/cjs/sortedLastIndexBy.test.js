"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const sortedLastIndex_ts_1 = require("./sortedLastIndex.js");
describe("sortedLastIndexBy", () => {
    it("handles an empty array", () => {
        const array = [];
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 0n, (x) => x)).toEqual(0);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 42n, (x) => x)).toEqual(0);
    });
    it("handles a contiguous array", () => {
        const array = [
            [1n, 42n],
            [2n, 42n],
            [3n, 42n],
        ];
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 0n, (x) => x)).toEqual(0);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 1n, (x) => x)).toEqual(1);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 2n, (x) => x)).toEqual(2);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 3n, (x) => x)).toEqual(3);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 4n, (x) => x)).toEqual(3);
    });
    it("handles a sparse array", () => {
        const array = [
            [1n, 42n],
            [3n, 42n],
        ];
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 0n, (x) => x)).toEqual(0);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 1n, (x) => x)).toEqual(1);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 2n, (x) => x)).toEqual(1);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 3n, (x) => x)).toEqual(2);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 4n, (x) => x)).toEqual(2);
    });
    it("handles negation", () => {
        const array = [
            [1n, 42n],
            [2n, 42n],
            [3n, 42n],
            [4n, 42n],
        ];
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 0n, (x) => -x)).toEqual(4);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 1n, (x) => -x)).toEqual(4);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 2n, (x) => -x)).toEqual(4);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 3n, (x) => -x)).toEqual(4);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 4n, (x) => -x)).toEqual(0);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 5n, (x) => -x)).toEqual(0);
    });
    it("handles a contiguous array with duplicate times", () => {
        const array = [
            [1n, 42n],
            [2n, 42n],
            [2n, 42n],
            [2n, 42n],
            [3n, 42n],
            [3n, 42n],
        ];
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 0n, (x) => x)).toEqual(0);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 1n, (x) => x)).toEqual(1);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 2n, (x) => x)).toEqual(4);
        expect((0, sortedLastIndex_ts_1.sortedLastIndexBy)(array, 3n, (x) => x)).toEqual(6);
    });
});
//# sourceMappingURL=sortedLastIndexBy.test.js.map