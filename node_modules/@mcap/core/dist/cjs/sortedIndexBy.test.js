"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const sortedIndexBy_ts_1 = require("./sortedIndexBy.js");
describe("sortedIndexBy", () => {
    it("handles an empty array", () => {
        const array = [];
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 0n, (x) => x)).toEqual(0);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 42n, (x) => x)).toEqual(0);
    });
    it("handles a contiguous array", () => {
        const array = [
            [1n, 42n],
            [2n, 42n],
            [3n, 42n],
        ];
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 0n, (x) => x)).toEqual(0);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 1n, (x) => x)).toEqual(0);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 2n, (x) => x)).toEqual(1);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 3n, (x) => x)).toEqual(2);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 4n, (x) => x)).toEqual(3);
    });
    it("handles a sparse array", () => {
        const array = [
            [1n, 42n],
            [3n, 42n],
        ];
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 0n, (x) => x)).toEqual(0);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 1n, (x) => x)).toEqual(0);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 2n, (x) => x)).toEqual(1);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 3n, (x) => x)).toEqual(1);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 4n, (x) => x)).toEqual(2);
    });
    it("handles negation", () => {
        const array = [
            [1n, 42n],
            [2n, 42n],
            [3n, 42n],
            [4n, 42n],
        ];
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 0n, (x) => -x)).toEqual(4);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 1n, (x) => -x)).toEqual(4);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 2n, (x) => -x)).toEqual(4);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 3n, (x) => -x)).toEqual(0);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 4n, (x) => -x)).toEqual(0);
        expect((0, sortedIndexBy_ts_1.sortedIndexBy)(array, 5n, (x) => -x)).toEqual(0);
    });
});
//# sourceMappingURL=sortedIndexBy.test.js.map