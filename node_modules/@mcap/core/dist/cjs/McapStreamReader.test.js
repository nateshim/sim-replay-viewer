"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const crc_1 = require("@foxglove/crc");
const McapRecordBuilder_ts_1 = require("./McapRecordBuilder.js");
const McapStreamReader_ts_1 = require("./McapStreamReader.js");
const constants_ts_1 = require("./constants.js");
const testUtils_ts_1 = require("./testUtils.js");
describe("McapStreamReader", () => {
    it("rejects invalid header", () => {
        for (let i = 0; i < constants_ts_1.MCAP_MAGIC.length - 1; i++) {
            const reader = new McapStreamReader_ts_1.default();
            const badMagic = constants_ts_1.MCAP_MAGIC.slice();
            badMagic[i] = 0x00;
            reader.append(new Uint8Array([...badMagic]));
            expect(() => reader.nextRecord()).toThrow("Expected MCAP magic");
        }
    });
    it("rejects invalid footer magic", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0x01234567), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC.slice(0, constants_ts_1.MCAP_MAGIC.length - 1),
            0x00,
        ]));
        expect(() => reader.nextRecord()).toThrow("Expected MCAP magic");
    });
    it("includes library in error messages", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.HEADER, [...(0, testUtils_ts_1.string)("prof"), ...(0, testUtils_ts_1.string)("lib")]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0x01234567), // summary crc
            ]),
            ...[0, 0, 0, 0, 0, 0, 0, 0],
        ]));
        expect(reader.nextRecord()).toEqual({ type: "Header", profile: "prof", library: "lib" });
        expect(() => reader.nextRecord()).toThrow(/Expected MCAP magic.+\[library=lib\]/);
    });
    it("includes 'no header' in error messages if there is no header", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0x01234567), // summary crc
            ]),
            ...[0, 0, 0, 0, 0, 0, 0, 0],
        ]));
        expect(() => reader.nextRecord()).toThrow(/Expected MCAP magic.+\[no header\]/);
    });
    it("parses empty file", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0x01234567), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(reader.nextRecord()).toEqual({
            type: "Footer",
            summaryStart: 0x0123456789abcdefn,
            summaryOffsetStart: 0x0123456789abcdefn,
            summaryCrc: 0x01234567,
        });
        expect(reader.done()).toBe(true);
    });
    it("accepts empty chunks", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHUNK, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // start_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // end_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // decompressed size
                ...(0, testUtils_ts_1.uint32LE)(0), // decompressed crc32
                ...(0, testUtils_ts_1.string)("lz4"), // compression
                ...(0, testUtils_ts_1.uint64LE)(BigInt(0n)),
                // no chunk data
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.DATA_END, [
                ...(0, testUtils_ts_1.uint32LE)(0), // data section crc
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(reader.nextRecord()).toEqual({
            type: "DataEnd",
            dataSectionCrc: 0,
        });
        expect(reader.nextRecord()).toEqual({
            type: "Footer",
            summaryStart: 0n,
            summaryOffsetStart: 0n,
            summaryCrc: 0,
        });
        expect(reader.done()).toBe(true);
    });
    it("waits patiently to parse one byte at a time, and rejects new data after read completed", () => {
        const reader = new McapStreamReader_ts_1.default();
        const data = new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0x01234567), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]);
        for (let i = 0; i < data.length - 1; i++) {
            reader.append(new Uint8Array(data.buffer, i, 1));
            expect(reader.nextRecord()).toBeUndefined();
            expect(reader.done()).toBe(false);
        }
        reader.append(new Uint8Array(data.buffer, data.length - 1, 1));
        expect(reader.nextRecord()).toEqual({
            type: "Footer",
            summaryStart: 0x0123456789abcdefn,
            summaryOffsetStart: 0x0123456789abcdefn,
            summaryCrc: 0x01234567,
        });
        expect(reader.done()).toBe(true);
        expect(() => {
            reader.append(new Uint8Array([42]));
        }).toThrow("Already done reading");
    });
    it("rejects extraneous data at end of file", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0x0123456789abcdefn), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0x01234567), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
            42,
        ]));
        expect(() => reader.nextRecord()).toThrow("bytes remaining after MCAP footer");
    });
    it("parses file with empty chunk", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHUNK, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // start_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // end_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // decompressed size
                ...(0, testUtils_ts_1.uint32LE)(0), // decompressed crc32
                ...(0, testUtils_ts_1.string)(""), // compression
                ...(0, testUtils_ts_1.uint64LE)(BigInt(0n)),
                // (no chunk data)
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(reader.nextRecord()).toEqual({
            type: "Footer",
            summaryStart: 0n,
            summaryOffsetStart: 0n,
            summaryCrc: 0,
        });
        expect(reader.done()).toBe(true);
    });
    it("rejects chunk with incomplete record", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHUNK, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // start_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // end_time
                ...(0, testUtils_ts_1.uint64LE)(1n), // decompressed size
                ...(0, testUtils_ts_1.uint32LE)((0, crc_1.crc32)(new Uint8Array([constants_ts_1.Opcode.CHANNEL]))), // decompressed crc32
                ...(0, testUtils_ts_1.string)(""), // compression
                ...(0, testUtils_ts_1.uint64LE)(BigInt(1n)),
                constants_ts_1.Opcode.CHANNEL, // truncated record
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(() => reader.nextRecord()).toThrow("bytes remaining in chunk");
    });
    it("rejects message at top level with no prior channel", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.MESSAGE, [
                ...(0, testUtils_ts_1.uint16LE)(42), // channel id
                ...(0, testUtils_ts_1.uint32LE)(0), // sequence
                ...(0, testUtils_ts_1.uint64LE)(0n), // log time
                ...(0, testUtils_ts_1.uint64LE)(0n), // publish time
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(() => reader.nextRecord()).toThrow("Encountered message on channel 42 without prior channel record");
    });
    it("rejects message in chunk with no prior channel", () => {
        const message = (0, testUtils_ts_1.record)(constants_ts_1.Opcode.MESSAGE, [
            ...(0, testUtils_ts_1.uint16LE)(42), // channel id
            ...(0, testUtils_ts_1.uint32LE)(0), // sequence
            ...(0, testUtils_ts_1.uint64LE)(0n), // log time
            ...(0, testUtils_ts_1.uint64LE)(0n), // publish time
        ]);
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHUNK, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // start_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // end_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // decompressed size
                ...(0, testUtils_ts_1.uint32LE)((0, crc_1.crc32)(message)), // decompressed crc32
                ...(0, testUtils_ts_1.string)(""), // compression
                ...(0, testUtils_ts_1.uint64LE)(BigInt(message.byteLength)),
                ...message,
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(() => reader.nextRecord()).toThrow("Encountered message on channel 42 without prior channel");
    });
    it("rejects schema data with incorrect length prefix", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.SCHEMA, [
                ...(0, testUtils_ts_1.uint16LE)(42), // id
                ...(0, testUtils_ts_1.string)("name"), // name
                ...(0, testUtils_ts_1.string)("encoding"), // encoding
                ...(0, testUtils_ts_1.uint32LE)(3), // length prefix
                10,
                11,
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(() => reader.nextRecord()).toThrow("Schema data length 3 exceeds bounds of record");
    });
    it("rejects attachment data with incorrect length prefix", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.ATTACHMENT, (0, testUtils_ts_1.crcSuffix)([
                ...(0, testUtils_ts_1.uint64LE)(2n), // log time
                ...(0, testUtils_ts_1.uint64LE)(1n), // create time
                ...(0, testUtils_ts_1.string)("myFile"), // name
                ...(0, testUtils_ts_1.string)("text/plain"), // media type
                ...(0, testUtils_ts_1.uint64LE)(3n), // data length
                10,
                11,
            ])),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(() => reader.nextRecord()).toThrow("Attachment data length 3 exceeds bounds of record");
    });
    it("parses channel at top level", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHANNEL, [
                ...(0, testUtils_ts_1.uint16LE)(1), // channel id
                ...(0, testUtils_ts_1.uint16LE)(2), // schema id
                ...(0, testUtils_ts_1.string)("myTopic"), // topic
                ...(0, testUtils_ts_1.string)("utf12"), // message encoding
                ...(0, testUtils_ts_1.keyValues)(testUtils_ts_1.string, testUtils_ts_1.string, [["foo", "bar"]]), // user data
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(reader.nextRecord()).toEqual({
            type: "Channel",
            id: 1,
            schemaId: 2,
            topic: "myTopic",
            messageEncoding: "utf12",
            metadata: new Map([["foo", "bar"]]),
        });
        expect(reader.nextRecord()).toEqual({
            type: "Footer",
            summaryStart: 0n,
            summaryOffsetStart: 0n,
            summaryCrc: 0,
        });
        expect(reader.done()).toBe(true);
    });
    it.each([true, false])("parses channel in chunk (compressed: %s)", (compressed) => {
        const channel = (0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHANNEL, [
            ...(0, testUtils_ts_1.uint16LE)(1), // channel id
            ...(0, testUtils_ts_1.uint16LE)(2), // schema id
            ...(0, testUtils_ts_1.string)("myTopic"), // topic
            ...(0, testUtils_ts_1.string)("utf12"), // message encoding
            ...(0, testUtils_ts_1.keyValues)(testUtils_ts_1.string, testUtils_ts_1.string, [["foo", "bar"]]), // user data
        ]);
        const decompressHandlers = { xyz: () => channel };
        const reader = new McapStreamReader_ts_1.default(compressed ? { decompressHandlers } : undefined);
        const payload = compressed ? new TextEncoder().encode("compressed bytes") : channel;
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHUNK, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // start_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // end_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // decompressed size
                ...(0, testUtils_ts_1.uint32LE)((0, crc_1.crc32)(channel)), // decompressed crc32
                ...(0, testUtils_ts_1.string)(compressed ? "xyz" : ""), // compression
                ...(0, testUtils_ts_1.uint64LE)(BigInt(payload.byteLength)),
                ...payload,
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(reader.nextRecord()).toEqual({
            type: "Channel",
            id: 1,
            schemaId: 2,
            topic: "myTopic",
            messageEncoding: "utf12",
            metadata: new Map([["foo", "bar"]]),
        });
        expect(reader.nextRecord()).toEqual({
            type: "Footer",
            summaryStart: 0n,
            summaryOffsetStart: 0n,
            summaryCrc: 0,
        });
        expect(reader.done()).toBe(true);
    });
    describe.each(["unchunked file", "same chunk", "different chunks"])("rejects channel with the same id in %s", (testType) => {
        it.each([
            {
                key: "topic",
                channel2: (0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHANNEL, [
                    ...(0, testUtils_ts_1.uint16LE)(42), // channel id
                    ...(0, testUtils_ts_1.uint16LE)(1), // schema id
                    ...(0, testUtils_ts_1.string)("XXXXXXXX"), // topic
                    ...(0, testUtils_ts_1.string)("utf12"), // message encoding
                    ...(0, testUtils_ts_1.keyValues)(testUtils_ts_1.string, testUtils_ts_1.string, [["foo", "bar"]]), // user data
                ]),
            },
            {
                key: "encoding",
                channel2: (0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHANNEL, [
                    ...(0, testUtils_ts_1.uint16LE)(42), // channel id
                    ...(0, testUtils_ts_1.uint16LE)(1), // schema id
                    ...(0, testUtils_ts_1.string)("myTopic"), // topic
                    ...(0, testUtils_ts_1.string)("XXXXXXXX"), // message encoding
                    ...(0, testUtils_ts_1.keyValues)(testUtils_ts_1.string, testUtils_ts_1.string, [["foo", "bar"]]), // user data
                ]),
            },
            {
                key: "schema_id",
                channel2: (0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHANNEL, [
                    ...(0, testUtils_ts_1.uint16LE)(42), // channel id
                    ...(0, testUtils_ts_1.uint16LE)(0), // schema id
                    ...(0, testUtils_ts_1.string)("myTopic"), // topic
                    ...(0, testUtils_ts_1.string)("utf12"), // message encoding
                    ...(0, testUtils_ts_1.keyValues)(testUtils_ts_1.string, testUtils_ts_1.string, [["foo", "bar"]]), // user data
                ]),
            },
        ])("differing in $key", ({ channel2 }) => {
            const channel = (0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHANNEL, [
                ...(0, testUtils_ts_1.uint16LE)(42), // channel id
                ...(0, testUtils_ts_1.uint16LE)(1), // schema id
                ...(0, testUtils_ts_1.string)("myTopic"), // topic
                ...(0, testUtils_ts_1.string)("utf12"), // message encoding
                ...(0, testUtils_ts_1.keyValues)(testUtils_ts_1.string, testUtils_ts_1.string, [["foo", "bar"]]), // user data
            ]);
            const reader = new McapStreamReader_ts_1.default();
            reader.append(new Uint8Array([
                ...constants_ts_1.MCAP_MAGIC,
                ...(testType === "unchunked file"
                    ? [...channel, ...channel2]
                    : testType === "same chunk"
                        ? (0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHUNK, [
                            ...(0, testUtils_ts_1.uint64LE)(0n), // start_time
                            ...(0, testUtils_ts_1.uint64LE)(0n), // end_time
                            ...(0, testUtils_ts_1.uint64LE)(0n), // decompressed size
                            ...(0, testUtils_ts_1.uint32LE)((0, crc_1.crc32)(new Uint8Array([...channel, ...channel2]))), // decompressed crc32
                            ...(0, testUtils_ts_1.string)(""), // compression
                            ...(0, testUtils_ts_1.uint64LE)(BigInt(channel.byteLength + channel2.byteLength)),
                            ...channel,
                            ...channel2,
                        ])
                        : [
                            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHUNK, [
                                ...(0, testUtils_ts_1.uint64LE)(0n), // start_time
                                ...(0, testUtils_ts_1.uint64LE)(0n), // end_time
                                ...(0, testUtils_ts_1.uint64LE)(0n), // decompressed size
                                ...(0, testUtils_ts_1.uint32LE)((0, crc_1.crc32)(new Uint8Array(channel))), // decompressed crc32
                                ...(0, testUtils_ts_1.string)(""), // compression
                                ...(0, testUtils_ts_1.uint64LE)(BigInt(channel.byteLength)),
                                ...channel,
                            ]),
                            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHUNK, [
                                ...(0, testUtils_ts_1.uint64LE)(0n), // start_time
                                ...(0, testUtils_ts_1.uint64LE)(0n), // end_time
                                ...(0, testUtils_ts_1.uint64LE)(0n), // decompressed size
                                ...(0, testUtils_ts_1.uint32LE)((0, crc_1.crc32)(new Uint8Array(channel2))), // decompressed crc32
                                ...(0, testUtils_ts_1.string)(""), // compression
                                ...(0, testUtils_ts_1.uint64LE)(BigInt(channel2.byteLength)),
                                ...channel2,
                            ]),
                        ]),
                ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                    ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                    ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                    ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
                ]),
                ...constants_ts_1.MCAP_MAGIC,
            ]));
            expect(reader.nextRecord()).toEqual({
                type: "Channel",
                id: 42,
                schemaId: 1,
                topic: "myTopic",
                messageEncoding: "utf12",
                metadata: new Map([["foo", "bar"]]),
            });
            expect(() => reader.nextRecord()).toThrow(/Channel record for id 42 \(topic: (myTopic|XXXXXXXX)\) differs from previous channel record of the same id./);
        });
    });
    it("validates attachment crc", () => {
        const reader = new McapStreamReader_ts_1.default();
        reader.append(new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.ATTACHMENT, (0, testUtils_ts_1.crcSuffix)([
                ...(0, testUtils_ts_1.uint64LE)(2n), // log time
                ...(0, testUtils_ts_1.uint64LE)(1n), // create time
                ...(0, testUtils_ts_1.string)("myFile"), // name
                ...(0, testUtils_ts_1.string)("text/plain"), // media type
                ...(0, testUtils_ts_1.uint64PrefixedBytes)(new TextEncoder().encode("hello")), // data
            ])),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]));
        expect(reader.nextRecord()).toEqual({
            type: "Attachment",
            name: "myFile",
            logTime: 2n,
            createTime: 1n,
            mediaType: "text/plain",
            data: new TextEncoder().encode("hello"),
        });
        expect(reader.nextRecord()).toEqual({
            type: "Footer",
            summaryStart: 0n,
            summaryOffsetStart: 0n,
            summaryCrc: 0,
        });
        expect(reader.done()).toBe(true);
    });
    it("allows parsing to start at an offset into the file", () => {
        const channel = (0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHANNEL, [
            ...(0, testUtils_ts_1.uint16LE)(1), // channel id
            ...(0, testUtils_ts_1.uint16LE)(0), // schema id
            ...(0, testUtils_ts_1.string)("myTopic"), // topic
            ...(0, testUtils_ts_1.string)("plain/text"), // message encoding
            ...(0, testUtils_ts_1.keyValues)(testUtils_ts_1.string, testUtils_ts_1.string, []), // user data
        ]);
        const fullMcap = new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHUNK, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // start_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // end_time
                ...(0, testUtils_ts_1.uint64LE)(BigInt(channel.byteLength)), // decompressed size
                ...(0, testUtils_ts_1.uint32LE)(0), // decompressed crc32
                ...(0, testUtils_ts_1.string)(""), // compression
                ...(0, testUtils_ts_1.uint64LE)(BigInt(channel.byteLength)),
                ...channel,
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.DATA_END, [
                ...(0, testUtils_ts_1.uint32LE)(0), // data section crc
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]);
        const magicReader = new McapStreamReader_ts_1.default();
        magicReader.append(fullMcap.slice(constants_ts_1.MCAP_MAGIC.length));
        expect(() => magicReader.nextRecord()).toThrow("Expected MCAP magic");
        const reader = new McapStreamReader_ts_1.default({ noMagicPrefix: true, includeChunks: true });
        reader.append(fullMcap.slice(constants_ts_1.MCAP_MAGIC.length));
        expect(reader.nextRecord()).toEqual({
            type: "Chunk",
            messageStartTime: 0n,
            messageEndTime: 0n,
            uncompressedSize: BigInt(channel.byteLength),
            uncompressedCrc: 0,
            compression: "",
            records: channel,
        });
        expect(reader.nextRecord()).toEqual({
            type: "Channel",
            id: 1,
            schemaId: 0,
            topic: "myTopic",
            messageEncoding: "plain/text",
            metadata: new Map(),
        });
        expect(reader.nextRecord()).toEqual({
            type: "DataEnd",
            dataSectionCrc: 0,
        });
        expect(reader.nextRecord()).toEqual({
            type: "Footer",
            summaryStart: 0n,
            summaryOffsetStart: 0n,
            summaryCrc: 0,
        });
        expect(reader.done()).toBe(true);
    });
    it("correctly appends new data to internal buffer", () => {
        const streamReader = new McapStreamReader_ts_1.default({ includeChunks: true, noMagicPrefix: true });
        const recordBuilder = new McapRecordBuilder_ts_1.McapRecordBuilder();
        const channel = {
            id: 0,
            messageEncoding: "json",
            schemaId: 0,
            topic: "foo",
            metadata: new Map(),
        };
        const messageSize = 1_000;
        const messageRecordBytes = 1 + 8 + 2 + 4 + 8 + 8 + messageSize;
        const makeMessage = (fillNumber) => ({
            channelId: 0,
            data: new Uint8Array(messageSize).fill(fillNumber),
            logTime: 0n,
            publishTime: 0n,
            sequence: 0,
        });
        const channelByteSize = recordBuilder.writeChannel(channel);
        streamReader.append(recordBuilder.buffer);
        expect(streamReader.bytesRemaining()).toBe(Number(channelByteSize));
        expect(streamReader.nextRecord()).toEqual({ ...channel, type: "Channel" });
        expect(streamReader.bytesRemaining()).toBe(0);
        // Add some messages and append them to the reader.
        recordBuilder.reset();
        recordBuilder.writeMessage(makeMessage(1));
        recordBuilder.writeMessage(makeMessage(2));
        streamReader.append(recordBuilder.buffer);
        expect(streamReader.bytesRemaining()).toBe(2 * messageRecordBytes);
        // Add one more message. Nothing has been consumed yet, but the internal buffer should be
        // large enough to simply append the new data.
        recordBuilder.reset();
        recordBuilder.writeMessage(makeMessage(3));
        streamReader.append(recordBuilder.buffer);
        expect(streamReader.bytesRemaining()).toBe(3 * messageRecordBytes);
        // Read some (but not all) messages to forward the reader's internal offset
        expect(streamReader.nextRecord()).toEqual({ ...makeMessage(1), type: "Message" });
        expect(streamReader.nextRecord()).toEqual({ ...makeMessage(2), type: "Message" });
        expect(streamReader.bytesRemaining()).toBe(1 * messageRecordBytes);
        // Add more messages. This will cause existing data to be shifted to the beginning of the buffer.
        recordBuilder.reset();
        recordBuilder.writeMessage(makeMessage(4));
        recordBuilder.writeMessage(makeMessage(5));
        streamReader.append(recordBuilder.buffer);
        expect(streamReader.bytesRemaining()).toBe(3 * messageRecordBytes);
        expect(streamReader.nextRecord()).toEqual({ ...makeMessage(3), type: "Message" });
        expect(streamReader.nextRecord()).toEqual({ ...makeMessage(4), type: "Message" });
        expect(streamReader.nextRecord()).toEqual({ ...makeMessage(5), type: "Message" });
        expect(streamReader.bytesRemaining()).toBe(0);
    });
    it("yields records with unknown opcodes", () => {
        const chunkedUnknownRecord = (0, testUtils_ts_1.record)(0x81, [5, 6, 7, 8]);
        const fullMcap = new Uint8Array([
            ...constants_ts_1.MCAP_MAGIC,
            // custom op code
            ...(0, testUtils_ts_1.record)(0x80, [1, 2, 3, 4]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.CHUNK, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // start_time
                ...(0, testUtils_ts_1.uint64LE)(0n), // end_time
                ...(0, testUtils_ts_1.uint64LE)(BigInt(chunkedUnknownRecord.byteLength)), // decompressed size
                ...(0, testUtils_ts_1.uint32LE)((0, crc_1.crc32)(chunkedUnknownRecord)), // decompressed crc32
                ...(0, testUtils_ts_1.string)(""), // compression
                ...(0, testUtils_ts_1.uint64LE)(BigInt(chunkedUnknownRecord.byteLength)),
                ...chunkedUnknownRecord,
            ]),
            ...(0, testUtils_ts_1.record)(constants_ts_1.Opcode.FOOTER, [
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary start
                ...(0, testUtils_ts_1.uint64LE)(0n), // summary offset start
                ...(0, testUtils_ts_1.uint32LE)(0), // summary crc
            ]),
            ...constants_ts_1.MCAP_MAGIC,
        ]);
        const reader = new McapStreamReader_ts_1.default();
        reader.append(fullMcap);
        expect(reader.nextRecord()).toEqual({
            type: "Unknown",
            opcode: 0x80,
            data: new Uint8Array([1, 2, 3, 4]),
        });
        expect(reader.nextRecord()).toEqual({
            type: "Unknown",
            opcode: 0x81,
            data: new Uint8Array([5, 6, 7, 8]),
        });
        expect(reader.nextRecord()).toEqual({
            type: "Footer",
            summaryStart: 0n,
            summaryOffsetStart: 0n,
            summaryCrc: 0,
        });
        expect(reader.nextRecord()).toBeUndefined();
    });
});
//# sourceMappingURL=McapStreamReader.test.js.map